import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../../Common';

export default function ContactUs() {
  return (
    <RNContainer>
      <RNHeader title={'Contact Us'}></RNHeader>
    </RNContainer>
  );
}

const styles = StyleSheet.create({});
