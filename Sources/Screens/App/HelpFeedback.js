import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../../Common';

export default function HelpFeedback() {
  return (
    <RNContainer>
      <RNHeader title={'Help & Feedback'}></RNHeader>
    </RNContainer>
  );
}

const styles = StyleSheet.create({});
