import { StyleSheet, Text, View } from 'react-native';
import { RNContainer, RNHeader } from '../../Common';

export default function Setting() {
  return (
    <RNContainer>
      <RNHeader title={'Setting'} back={false}></RNHeader>
    </RNContainer>
  );
}

const styles = StyleSheet.create({});
