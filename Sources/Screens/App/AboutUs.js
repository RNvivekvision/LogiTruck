import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../../Common';

export default function AboutUs() {
  return (
    <RNContainer>
      <RNHeader title={'About Us'}></RNHeader>
    </RNContainer>
  );
}

const styles = StyleSheet.create({});
